'use client';

import LoginScreen from '../../components/LoginScreen'; // NOTE: .. not @/
// path is: app/login → app/components/LoginScreen.tsx

export default function LoginPage() {
  return <LoginScreen />;
}
